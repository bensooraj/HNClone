<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/style.css">
    <title>User: <%= userProfile.username %></title>
</head>
<body>
    <%- include('../header') -%>
    <br>
    user: <h3 class="inline"><%= userProfile.username %></h3>
    <strong>
        <nav class="TabMenu">
            |&nbsp;<a class="m-active" href="/user/<%= userProfile.username %>">Overview</a>&nbsp;
            |&nbsp;<a class="" href="/user/<%= userProfile.username %>/posts/">Posts</a>&nbsp;
            |&nbsp;<a class="" href="/user/<%= userProfile.username %>/comments/">Comments</a>&nbsp;
        </nav>
    </strong>
    <hr>
    <section class="posts-section">
        <h4 class="title">Posts</h4>
        <ul class="posts-list">
            <% if (userProfile.posts.length == 0) {%>
                <p>The user hasn't submitted any posts yet.</p>
            <%}%>
            <% for(var i=0; i<userProfile.posts.length; i++) {%>
                <li class="posts">
                    <h4> <a href="/posts/<%= userProfile.posts[i]._id %>">
                        <%= userProfile.posts[i].title %>
                    </a></h4>
                    <p>
                        <%= userProfile.posts[i].text %>
                    </p>
                </li>
            <% } %>
        </ul>
    </section>
</body>
</html>