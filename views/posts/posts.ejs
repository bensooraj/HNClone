<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/style.css">
    <title>All posts</title>
</head>
<body>
    <%- include('../header') -%>
    <h3>All posts:</h3>
    <hr>
    <ul class="posts">
        <% for(var i=0; i<posts.length; i++) {%>
            <li class="posts">
                <h4> <a href="/posts/<%= posts[i]._id %>">
                    <%= posts[i].title %>
                </a></h4>
                <strong>created at </strong><%= posts[i].createdAt %>
                <strong>by </strong> 
                <a href="/user/<%= posts[i].author.username %>"><%= posts[i].author.username %></a>
            </li>
        <% } %>
    </ul>
    <!-- <ul>
        <li><%= posts %> </li>
    </ul> -->
</body>
</html>